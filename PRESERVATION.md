# Preservation record

This collection contains 14 articles as readable text and privacy-screened HTML, with 97 image files. Research-folder paths in article bodies are replaced with `[RESEARCH_PATH_REDACTED]`. Account data is excluded.

`articles_manifest.json` records source URLs and checksums of the public text and HTML copies. `media_manifest.json` records image source URLs and retrieval results. The original September 5 collection’s 104 image references have preserved representations: 100 direct downloads and four browser-served WebP substitutes, totaling 95 unique files. The four corresponding PNG requests returned HTTP 429; their exact PNG bytes are unavailable here. Export resolutions may be smaller than original uploads. Linked pages and dynamic embeds are not full offline mirrors.

`SHA256SUMS.txt` covers every published file except itself. Checksums verify copy integrity, not the truth of article claims. See [verification notes](VERIFICATION.md). No new license is imposed on embedded third-party material.

## September 6 addition

[For Those Who Stayed Long Enough to Hear It](articles/a8d8da797a43/offline.html) is preserved from its published browser page, not the earlier account export. All 507 article blocks, including 14 code blocks and two illustrations, are represented. One image is the browser-served WebP rendition and one is the retrieved JPEG; these add two unique files. The total is 14 articles and 97 image files.

For this addition, `original.html` means the screened article-body snapshot: navigation, account controls, site classes and tracking attributes are excluded. It is not a byte-identical Medium server response. `preservation.json` records the acquisition and transformations. The published text is preserved as captured; its plaintext display loses a few whitespace bytes compared with the canonical decode. See the article’s verification note and separately labelled byte-exact reproduction files.
